"""DEPRECADO: as rotas de analise foram movidas para `app/routes/analyse.py`.

Use os endpoints:
  - POST /analyse/trained  (modelo treinado)
  - POST /analyse/base      (YOLO11 base / COCO)

Este arquivo foi mantido apenas como referencia e nao registra rotas.
"""
